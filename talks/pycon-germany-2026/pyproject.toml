[project]
name = "pycon-germany-2026"
version = "0.0.0"
requires-python = ">=3.10"
dependencies = [
    "nicegui>=3.0.0,<4.0.0",
    "nicedeck==0.2.0",
    "numpy>=2.2.6",
]

[tool.uv.sources]
nicedeck = { git = "https://github.com/falkoschindler/nicedeck.git", tag = "v0.2.0" }
